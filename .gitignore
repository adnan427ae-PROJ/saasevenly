# dependencies
/node_modules

# next.js build output
/.next
/out

# env files (never commit your real Stripe keys!)
.env.local
.env*.local

# locally cached exchange rates (regenerated automatically)
/data/rates.json

# local SQLite database (created on first run)
/data/saasevenly.db
/data/saasevenly.db-*

# misc
.DS_Store
*.log
.vercel
